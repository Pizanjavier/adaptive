{
  "name": "@adaptive/svelte",
  "version": "0.1.0",
  "description": "Svelte adapter for Adaptive — adaptive() API and stores",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@adaptive/core": "workspace:*",
    "svelte": ">=4.0.0"
  },
  "devDependencies": {
    "@adaptive/core": "workspace:*",
    "svelte": "^4.2.0"
  },
  "sideEffects": false
}
